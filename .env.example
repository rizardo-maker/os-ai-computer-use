# OS AI Configuration Example
# Copy this file to .env and fill in your values

# =============================================================================
# API KEYS (Required for CLI/Development)
# =============================================================================
# Note: GUI users should use the Settings screen instead of this file
# These environment variables are only used when running via CLI or for development

# Anthropic API Key (required)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI API Key (optional, for future use)
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here

# Azure OpenAI (optional)
# Use a deployment of the computer-use-preview model.
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=computer-use-preview
# AZURE_OPENAI_API_VERSION=2025-04-01-preview

# =============================================================================
# BACKEND CONFIGURATION (Optional)
# =============================================================================
# These settings are usually auto-configured, but can be overridden if needed

# Backend server host (default: 127.0.0.1)
# OS_AI_BACKEND_HOST=127.0.0.1

# Backend server port (default: 8765)
# OS_AI_BACKEND_PORT=8765

# =============================================================================
# DEVELOPMENT SETTINGS (Optional)
# =============================================================================

# Enable debug logging (default: false)
# DEBUG=true

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - NEVER commit .env to git
# - Keep your API keys secure
# - Rotate keys regularly
# - For production deployments, use environment variables or secret managers
# - GUI app users: API keys are stored in system keychain, not in .env
